machine:
    services:
        - docker

dependencies:
    override:
        - docker build -t test .

test:
    override:
        - docker run test
# test:
#   override:
#     - env | sort
#     - ./gradlew test
